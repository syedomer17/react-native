# react-native
This is my react native 


npx @react-native-community/cli init client --version 0.73.8 --skip-git
cd client
npm install
cd android
.\gradlew clean
cd ..
Start emulator manually (API 34).

Then:

npm run android